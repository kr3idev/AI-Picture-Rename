export const DEFAULT_PROMPT: string =
  "Generate a short and appropriate ${targetLanguage} filename based on the content of this image (no extension needed; if unable to generate a filename, create one using the most distinctive feature of the image; do not include any content other than the filename).";
export const PROJECT_URL: string =
  "https://github.com/kr3idev/AI-Picture-Rename";
